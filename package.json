{
  "name": "mono2",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "npm --prefix monologg run dev",
    "build": "npm --prefix monologg run build",
    "typecheck": "npm --prefix monologg run typecheck",
    "test": "npm --prefix monologg run test"
  }
}
