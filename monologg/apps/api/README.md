# @monologg/api

Empty scaffold, per `features.md` Phase 1. The real Fastify server, provider
interfaces, config, and routes land in Phase 3 onward — see `features.md` for
the full spec. Nothing in here runs yet.
